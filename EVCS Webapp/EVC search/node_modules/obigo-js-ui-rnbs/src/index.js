import install from './vue-install'
import standaloneInstall from './standalone-install'
import * as theme from './features/theme'

import Utils from './utils/index'

import Style from './styles/index.vue' // eslint-disable-line
import localStorage from './features/localStorage'
import aic from './features/aic'
import appManager from './features/appManager'
import './features/error-handler'

const obigoUI = {
  version: '0.1.0',
  install,
  theme
}

export {
  theme,
  Utils,
  localStorage,
  aic,
  appManager
}

standaloneInstall(obigoUI)

export default obigoUI
