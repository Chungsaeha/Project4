export default function (ObigoUI) {
  // auto install in standalone mode
  if (typeof window !== 'undefined' && window.Vue) {
    if (!ObigoUI.theme.current) {
      ObigoUI.theme.set('mat')
    }

    window.ObigoUI = ObigoUI
    window.Vue.use(ObigoUI)
  }
}
